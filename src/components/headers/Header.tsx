import React from 'react';

const Header: React.FC = () => (
  <header className="h-16 flex items-center px-6 shadow bg-white dark:bg-gray-900">
    <h1 className="font-bold text-lg text-primary">React Base Project</h1>
  </header>
);

export default Header;
